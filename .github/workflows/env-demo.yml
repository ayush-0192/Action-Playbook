name: Env Demo

on:
    push:
        branches:
            #- main
            - dev
env:
    APP_NAME: action-workflow
    ENVIRONMENT: dev

jobs:
    env-demo:
        runs-on: ubuntu-latest

        steps:
            - run: echo $APP_NAME
            - run: echo $ENVIRONMENT


